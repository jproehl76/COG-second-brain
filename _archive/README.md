# Archive

Destination for content confirmed stale by `memory-hygiene` or an account/people review. Nothing moves here automatically. Every move into this directory happens after explicit, batched confirmation, never one prompt at a time.

This is the fourth PARA-style category this vault's taxonomy is missing otherwise: Projects (`04-projects`), Areas (`02-personal`/`03-professional`), Resources (`05-knowledge`), and Archive (here). Content here has stopped counting as active without being deleted.
